import { createMuiTheme } from "@material-ui/core/styles";

export const materialTheme = createMuiTheme({
  palette: {
    primary: {
      main: "#252934",
    },
    secondary: {
      main: "#fefefe",
    },
  },
});
